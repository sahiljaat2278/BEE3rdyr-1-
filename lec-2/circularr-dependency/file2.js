const f1=require("./file1.mjs");
console.log(f1);

 function sum(a, b) {
  return a + b;

  //WHY THREADPOOL IS USED
  //Threadpool is used in Node.js to handle asynchronous operations that are CPU-intensive or blocking, allowing the event loop to continue processing other tasks without being blocked by these operations.
}
